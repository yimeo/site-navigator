# 网站导航系统设计说明

本系统采用 **NGINX + PHP 7.0 + SQLite** 的无外部数据库部署方式，面向中小规模网站导航场景。前台从分类、搜索、网站卡片和排序四个层级组织内容；后台负责分类优先级、网站资料、主备域名、图标同步和运行状态的维护。页面借鉴参考站的“固定分类导航 + 分区网站卡片 + 快速搜索”模式，但不复用其内容、品牌或视觉资源。

| 模块 | 设计 | 核心行为 |
| --- | --- | --- |
| 分类 | `categories` 表中的 `sort_order` 决定优先级 | 数值越小越靠前，管理端支持上移与下移 |
| 网站 | `websites` 表保存自动抓取或人工修订后的标题、简介、分类、主域名、备用域名与展示优先级 | 后台仅需输入目标 URL；其余内容自动解析，仍支持必要时人工覆写 |
| 排序 | 前台采用 `priority`、`popular`、`latest` 三种模式 | 人气按有效跳转累计值降序；最新按创建时间降序；默认按后台设置顺序 |
| 主备切换 | 跳转入口统一经过 `go.php` | 先检测主地址；不可访问时检测并跳转备用地址；两者均失效时返回友好错误页 |
| 健康状态 | `website_health` 表记录主、备地址最近检测的状态、HTTP 状态码、错误、响应耗时与检测时间 | 定时脚本批量检测；用户跳转时亦会更新即时结果 |
| 图标同步 | 图标服务从目标站首页解析 `rel=icon`，并回退到 `/favicon.ico` | 仅接收受控大小的图像类型；存到 `public/assets/icons/` 并在数据库记录本地路径 |
| 安全 | 后台采用 session 登录、密码哈希、CSRF 令牌和预处理 SQL | 仅允许 HTTP/HTTPS URL，拒绝本机、私网和保留地址，防止服务端请求伪造 |

## URL 自动录入流程

后台新增网站时，管理员只需在“目标 URL”字段粘贴完整的 `http://` 或 `https://` 地址。系统先校验协议、解析主机名并拦截本机、私有网络及保留地址；通过校验后，以短超时访问目标页面。解析器优先读取 HTML 的 `og:title` 和 `og:description`，其次读取 `<title>`、`meta[name="description"]` 与页面首段可见文本作为介绍兜底。图标解析优先读取 `rel="icon"` 或 `rel="shortcut icon"`，没有声明时再请求站点根目录的 `/favicon.ico`；确认安全的图片响应后保存至本地图标目录。

自动解析结果会先呈现在确认页面，管理员可直接保存，也可在保存前覆写标题、介绍、分类、备用域名和显示优先级。系统会把原始目标 URL 设为主地址，并用规范化后的地址参与后续可用性检测。对抓取超时、站点拒绝访问、返回非 HTML 或无描述的情形，表单保留 URL 并给出明确提示，允许管理员补充内容后继续保存。

| 自动读取项 | 首选来源 | 备用来源 | 保存位置 |
| --- | --- | --- | --- |
| 网站标题 | `og:title` | `<title>`、主机名 | `websites.title` |
| 网站介绍 | `og:description` | `meta[name=description]`、页面正文摘要 | `websites.description` |
| 站点图标 | 页面图标链接 | `/favicon.ico` | `public/assets/icons/` 与 `websites.icon_path` |
| 主域名 | 输入的目标 URL | 无 | `websites.primary_url` |

## 主要数据关系


```text
categories (1) ────< websites (1) ──── (1) website_health
                       │
                       └────< redirect_logs

users 用于后台登录；settings 用于站点名称和运行参数。
```

## 自动任务与部署策略

项目提供两个命令行脚本。`cron/check-sites.php` 以有限并发逐一检查已启用网站的主、备用地址，更新健康状态；`cron/sync-icons.php` 同步缺失、过期或指定网站的图标。生产环境可由系统计划任务每 10 分钟执行健康检查、每天执行一次图标同步。该逻辑完全由确定性代码完成，不依赖人工干预或模型调用。

| 任务 | 建议频率 | 结果 |
| --- | --- | --- |
| 健康检测 | 每 10 分钟 | 更新主、备用地址可用性，为跳转优先选择提供缓存依据 |
| 图标同步 | 每天一次 | 为新站点或图标失效站点保存可本地访问的图标文件 |
| 跳转日志清理 | 每月一次 | 按需要清理历史统计细节，保留聚合人气值 |

## 兼容性约束

应用代码仅使用 PHP 7.0 已提供的语言和标准扩展能力，并依赖 `PDO_SQLITE` 与 `cURL`。为在当前开发环境中执行自动化验证，可以使用更高版本 PHP 运行兼容性测试，但最终 NGINX 配置明确指向 `php7.0-fpm`；部署服务器必须自行安装并启用 PHP 7.0 的 SQLite 与 cURL 扩展。由于 PHP 7.0 已停止维护，生产部署应位于受控、隔离的运行环境，并在条件允许时计划升级至受支持版本。
